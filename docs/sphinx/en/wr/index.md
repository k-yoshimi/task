# `wr` — Wave Ray

```{admonition} Coming soon
:class: note

Full chapter in preparation. For now, see:

- Python wrapper — `python/wrlib/README.md`
- Fortran design — `docs/wr-library/architecture.md`
- MCP server — `python/mcp-servers/wr_mcp/README.md`
```

## Overview

`wr` is the geometric-optics ray tracing solver for RF heating / current
drive.  The sibling module `wrx` extends `wr` with beam-tracing options.

## Package docstring (autodoc)

```{eval-rst}
.. automodule:: wrlib
   :no-members:
```
