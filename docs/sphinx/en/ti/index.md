# `ti` — Transport-Integrated

```{admonition} Coming soon
:class: note

Full chapter in preparation. For now, see:

- Python wrapper — `python/tilib/README.md`
- Fortran design — `docs/ti-library/architecture.md`
- MCP server — `python/mcp-servers/ti_mcp/README.md`
```

## Overview

`ti` is the integrated transport interface that couples the transport
solver (`tr`) with auxiliary physics models. It shares the 5-function
C ABI described in the {doc}`../common/architecture` chapter.

## Package docstring (autodoc)

```{eval-rst}
.. automodule:: tilib
   :no-members:
```
